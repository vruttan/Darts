{
  "name": "darts-tournament-pwa",
  "private": true,
  "type": "module",
  "description": "Static offline-first PWA for running double-elimination darts tournaments. No build step; this file only exists so `node tests/bracket.test.js` can run the bracket module as native ES modules.",
  "scripts": {
    "test": "node tests/bracket.test.js"
  }
}
